package model.game.products.finalproducts;

import controller.mission.time.TimeManager;

public class IceCream extends FinalProduct{
    public IceCream(TimeManager timeManager) {
        super(timeManager);
    }
}
