package model.game.products.finalproducts;

import controller.mission.time.TimeManager;

public class Shirt extends FinalProduct{
    public Shirt(TimeManager timeManager) {
        super(timeManager);
    }
}
