package model.game.products.CagedPredators;

import controller.mission.time.TimeManager;

public class CagedLion extends CagedPredator{
    public CagedLion(TimeManager timeManager, int x, int y) {
        super(timeManager, x, y);
    }
}
