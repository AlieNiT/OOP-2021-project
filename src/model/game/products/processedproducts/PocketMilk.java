package model.game.products.processedproducts;

import controller.mission.time.TimeManager;

public class PocketMilk extends ProcessedProduct{
    public PocketMilk(TimeManager timeManager) {
        super(timeManager);
    }
}
