package model.game.products.processedproducts;

import controller.mission.time.TimeManager;

public class Flour extends ProcessedProduct {
    public Flour(TimeManager timeManager, int x, int y) {
        super(timeManager,x,y);
    }
}
