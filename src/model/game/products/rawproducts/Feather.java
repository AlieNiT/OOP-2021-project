package model.game.products.rawproducts;

import controller.mission.time.TimeManager;

public class Feather extends RawProduct{
    public Feather(TimeManager timeManager, int x, int y) {
        super(timeManager,x,y);
    }
}
